<% if @current_user %>
<h1><%= @current_user.first_name.capitalize %>'s lists</h1>

<ul>
  <% @current_user.lists.each do |list| %>
  <li>
    <%= link_to list.title, list_items_url(list) %>
   <% end %>
  </li>
</ul>
<% end %>

<%= link_to 'New List', new_list_path %>
