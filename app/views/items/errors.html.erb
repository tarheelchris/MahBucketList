
NEW
<% if @current_user.lists.find(params[:list_id]).items.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_user.lists.find(params[:list_id]).items.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @current_user.lists.find(params[:list_id]).items.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  EDIT
  
  <% if @current_user.lists.find(params[:list_id]).items.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@current_user.lists.find(params[:list_id]).items.errors.count, "error") %> prohibited this item from being saved:</h2>

        <ul>
        <% @current_user.lists.find(params[:list_id]).items.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  